package controller;

import model.Store;
//import view.StoreView.StoreView;

public class StoreController {
    Store store;

    public StoreController(Store store){
        this.store = store;
    }

    // 아래에 메서드 호출
}
